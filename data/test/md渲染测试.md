# 一级标题

## 二级标题

### 三级标题

#### 四级标题

---

## 文字样式

这是普通文本。

这里有 **粗体文字** 和 *斜体文字*，还有 `行内代码`。

混合使用：**粗体中包含 `代码`** 以及 *斜体结尾*。

---

## 列表

- 第一项
- 第二项
- 第三项内容稍长，用来测试换行显示是否正常

* 星号列表一
* 星号列表二

---

## 代码块

```
fn hello() {
    println!("Hello, world!");
}
```

---

## 引用

> 这是一段引用文字，通常用来引用他人的话或重要提示。

> 第二段引用内容。

---

## 综合段落

本工具使用 **Rust** 编写，基于 `ratatui` 构建 TUI 界面。  
按 `Tab` 键可以在 *原始文本* 和 **MD 渲染** 两种模式之间切换。  
按 `y` 复制内容，按 `q` 或 `Esc` 返回列表。
